I am seeing double-counted station readings under /app after the registry update. 
I need a small Python script at /app/dedupe_report.py whose first line is exactly `#!/usr/bin/env python3`, marked executable (`chmod +x`) so it can be run as a normal program. 
It should rebuild outputs from /app/inputs/station_readings.csv and /app/inputs/station_registry.json only, create /app/output when needed, and write /app/output/deduped.csv plus /app/output/stats.json. Please don’t read or import anything under /tests, /oracle, /solution, or /logs at runtime; the harness runs your script with its working directory set to /app.

The input CSV might start with a UTF-8 BOM. Treat it as having timestamp, station_id, temperature_c, and quality_code, and strip ASCII whitespace on every field. Skip malformed rows into skipped_malformed_rows when the timestamp or station id is empty, temperature_c is blank or not a finite float, quality isn’t exactly OK, WARN, or EST, or the timestamp can’t be parsed. Before you interpret timestamps, resolve station_id through the registry aliases (follow chains until they stop; if you hit a cycle, the canonical id is the lexicographically smallest id in that cycle). Parse times with datetime.fromisoformat after rewriting a trailing Z to +00:00; keep aware instants in UTC. Naive instants are local wall times for the canonical station using its IANA entry in station_timezones with zoneinfo, and if that station isn’t listed there, assume UTC. On a fall-back DST ambiguity, take the later UTC instant. On a spring-forward gap, nudge the naive clock forward one minute at a time until it exists locally, count that in shifted_nonexistent_timestamps, and don’t call the row malformed unless something else is wrong. For each row that makes it past parsing, use its final UTC instant: if it falls in any suppression window for that station (UTC, inclusive start and exclusive end, same half-open rule as calibration), skip it and count skipped_suppressed_rows; otherwise add the calibration offset for that instant before you keep the row for dedupe. Calibration windows are UTC with inclusive start and exclusive end; add the matching offset_c, and if several windows match, pick the latest start, then the later entry in the registry file if starts still tie. Deduplicate on canonical station id plus UTC instant, letting the last surviving row in file order win. Emit deduped.csv with header columns in this order: timestamp, station_id, temperature_c, quality_code. Keep the winner’s stripped timestamp text, write the canonical station id, and format temperature_c to one decimal place using round then str (write -0.0 as 0.0). Sort rows by normal string order of station_id, then by UTC instant.

Write stats.json as normal JSON with duplicate_rows_dropped, skipped_malformed_rows, skipped_suppressed_rows, shifted_nonexistent_timestamps, deduped_row_count, station_count, median_temperature_c_all, global_quality_counts, and stations. Both global_quality_counts and each station’s quality_counts must be plain objects with exactly OK, WARN, and EST as integer counts. The stations array should be sorted by station_id. Each station object needs station_id, readings (integer count of kept rows for that station), min and max and median and average temperature fields as strings, quality_counts, longest_gap_minutes as the biggest whole-minute gap between consecutive kept UTC times for that station (or null if there’s only one point), and quality_runs as a single JSON integer, not a list: sort that station’s kept readings by UTC, walk in order, and use one plus how many times the quality_code changes from the previous row (one reading gives you 1). For medians over numbers, sort them and average the two middle values when there’s an even count. Use the same one-decimal string style for min, max, median, and average fields on each station and for median_temperature_c_all as you used for temperature_c in the CSV.
