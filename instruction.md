I am getting double-counted station readings under `/app` after a registry update. Write executable `/app/dedupe_report.py` with first line exactly `#!/usr/bin/env python3`; when run from `/app`, it may only read `/app/inputs/station_readings.csv` and `/app/inputs/station_registry.json`, must not read or import from `/tests`, `/oracle`, `/solution`, or `/logs`, and must create `/app/output/deduped.csv` plus `/app/output/stats.json`.

Use only the Python standard library; do not use `eval`, `exec`, `compile`, `__import__`, or third-party helpers such as numpy, pandas, pytz, or dateutil. Read CSV columns `timestamp`, `station_id`, `temperature_c`, `quality_code` with BOM-safe UTF-8 decoding, strip ASCII whitespace from fields, and skip/increment `skipped_malformed_rows` for blank timestamp/station, blank/non-float/non-finite temperature, quality not exactly `OK`, `WARN`, or `EST`, or an unparseable timestamp. Resolve station aliases before timezone work; aliases may chain, and cycles collapse to the lexicographically smallest id in the cycle. Use the canonical id for timezone, suppression, calibration, dedupe, and output.

Parse timestamps with `datetime.fromisoformat` after rewriting trailing `Z` to `+00:00`. Convert aware datetimes to UTC. Interpret naive datetimes in the canonical station timezone from `station_timezones` after resolving timezone keys through aliases; default missing zones to `UTC`, and if multiple entries resolve to one canonical id, the later registry entry wins. For ambiguous fall-back wall times choose the later UTC instant. For nonexistent spring-forward wall times, nudge forward one minute at a time until valid, increment `shifted_nonexistent_timestamps` once for that row, then continue even if the shifted instant is later suppressed.

After computing the final UTC instant, process rows in this order: suppress if the canonical station and instant are inside any UTC half-open suppression window, incrementing `skipped_suppressed_rows`; otherwise apply calibration for matching UTC half-open windows by adding `offset_c`, choosing the latest `start` and then the later registry entry on ties; then deduplicate by `(canonical station id, final UTC instant)` with the last row in file order winning. Hidden inputs may be large, so avoid quadratic full-dataset passes.

Write `/app/output/deduped.csv` with header `timestamp,station_id,temperature_c,quality_code`, sorted by station_id then UTC instant; each row must contain the winning stripped input timestamp string, canonical station id, calibrated temperature formatted as `str(round(x, 1))` with `-0.0` mapped to `0.0`, and quality code. Write `/app/output/stats.json` with exactly these top-level keys: `duplicate_rows_dropped`, `skipped_malformed_rows`, `skipped_suppressed_rows`, `shifted_nonexistent_timestamps`, `deduped_row_count`, `station_count`, `median_temperature_c_all`, `global_quality_counts`, `stations`; the first six are integers, duplicate drops count non-suppressed well-formed rows lost to dedupe, global quality counts has exactly `OK`, `WARN`, and `EST`, and stations is sorted by station_id. Each station entry must have exactly `station_id`, `readings`, `min_temperature_c`, `max_temperature_c`, `median_temperature_c`, `avg_temperature_c`, `quality_counts`, `longest_gap_minutes`, and `quality_runs`; compute station and global medians by sorting numeric values and averaging the two middle values when needed, averages use the arithmetic mean, `quality_runs` is 1 plus quality-code changes in UTC order, `longest_gap_minutes` is the largest whole-minute gap between consecutive UTC instants or null for one reading, all temperature strings use the same one-decimal formatting, and per-station quality counts include exactly `OK`, `WARN`, and `EST`.
