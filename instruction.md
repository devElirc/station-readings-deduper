The station readings job in /app started double-counting after the station registry changed. Add an executable Python 3 script at /app/dedupe_report.py with #!/usr/bin/env python3. It must read only /app/inputs/station_readings.csv and /app/inputs/station_registry.json, create /app/output if needed, and write /app/output/deduped.csv and /app/output/stats.json. At runtime it must not open, import, or otherwise read paths under /tests, /oracle, /solution, or /logs. Verification runs the script with working directory /app. The CSV may have a UTF-8 BOM; columns are timestamp, station_id, temperature_c, quality_code. Strip ASCII whitespace from every field. Malformed rows (count skipped_malformed_rows) have empty timestamp or station_id, blank or non-finite float temperature_c, quality_code not in OK/WARN/EST, or an unparseable timestamp. Canonicalize station_id via registry aliases (resolve chains; cycles collapse to the lexicographically smallest id in the cycle) before interpreting timestamps. Parse timestamps with datetime.fromisoformat after turning a trailing Z into +00:00; convert aware values to UTC. Naive values use the canonical station's IANA zone from station_timezones; if the canonical station has no timezone entry, default to UTC. Use zoneinfo. On DST fall-back ambiguity pick the later UTC instant; on spring-forward gaps shift the naive wall time forward one minute at a time until valid, count shifted_nonexistent_timestamps, and do not treat that as malformed unless another rule fails. If the final UTC instant falls in a suppression window for that station, skip the row and count skipped_suppressed_rows. Apply calibration after suppression and before dedupe: UTC windows have inclusive start and exclusive end; add offset_c for the matching window, breaking ties by latest start then later registry entry order. Dedupe on (canonical station_id, UTC instant) with last non-skipped row in file order winning. Write deduped.csv with columns timestamp, station_id, temperature_c, quality_code using the winning row's stripped timestamp string, canonical station_id, str() of the calibrated float, sorted by default string station_id then UTC instant.

stats.json must include duplicate_rows_dropped, skipped_malformed_rows, skipped_suppressed_rows, shifted_nonexistent_timestamps, deduped_row_count, station_count, median_temperature_c_all, global_quality_counts, and stations. Quality count objects must have exactly OK, WARN, and EST keys with integers. stations is sorted by station_id; each entry has station_id, readings, min_temperature_c, max_temperature_c, median_temperature_c, avg_temperature_c, quality_counts, longest_gap_minutes (largest whole-minute gap between consecutive retained UTC instants for that station, or null for a single reading), and quality_runs (contiguous quality-code runs after sorting that station's readings by UTC instant). Medians sort numeric values and average the two middle values when the count is even. Format min, max, median, avg, and median_temperature_c_all as str(round(x, 1)) with -0.0 shown as 0.0.
