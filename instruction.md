We’re getting double-counted station readings under `/app` after the registry update, and I need a small Python script at `/app/dedupe_report.py` to clean it up. The file’s first line should be exactly `#!/usr/bin/env python3`, and it should be marked executable with `chmod +x` so it runs like a normal program. It should only read `/app/inputs/station_readings.csv` and `/app/inputs/station_registry.json`, create `/app/output` if that folder isn’t there yet, and write `/app/output/deduped.csv` plus `/app/output/stats.json`. Please don’t read or import anything under `/tests`, `/oracle`, `/solution`, or `/logs` while the script runs; the harness will run you with working directory `/app`.

The CSV might start with a UTF-8 BOM, and the columns are timestamp, station_id, temperature_c, and quality_code—strip ASCII whitespace on every field after you read it. Bump `skipped_malformed_rows` when the timestamp or station id is empty after stripping, when temperature_c is blank or not a finite float, when quality isn’t exactly OK, WARN, or EST, or when you can’t parse the time. Before you parse times, walk the registry aliases to a canonical station id (chains can keep going; if you hit a cycle, use the lexicographically smallest id in the cycle). Parse with `datetime.fromisoformat` after turning a trailing Z into +00:00, send aware instants to UTC, and treat naive instants as local wall time for the canonical station using `zoneinfo` and its entry in `station_timezones`, defaulting to UTC when that station isn’t listed. On a fall-back DST ambiguity, pick the later UTC instant. On a spring-forward gap, nudge the naive clock forward one minute at a time until the local time exists, then use that instant, and add one to `shifted_nonexistent_timestamps` for each input row that needed at least one such nudge (count whole rows, not each minute step); do this before you test suppression, so a row that is later skipped as suppressed still counts if it was shifted. Those rows aren’t malformed unless something else already failed. For each row that survives parsing, take its final UTC instant: if it falls inside a suppression window for that canonical station (UTC, inclusive start and exclusive end, same rule as calibration), skip it and count `skipped_suppressed_rows`; otherwise add the calibration offset for that instant before you keep it for dedupe. Calibration windows are also UTC half-open intervals; add the matching offset_c, and if several windows match, prefer the latest start, then the later entry in the registry file if starts still tie. Finally dedupe on canonical station id plus UTC instant so the last row in file order wins, and build both outputs from that deduped set.

For `deduped.csv`, use the header columns timestamp, station_id, temperature_c, and quality_code in that order. For each kept row, keep the winner’s stripped timestamp string, write the canonical station id, and write temperature_c as a one-decimal string from round then str (write -0.0 as 0.0). Sort rows by normal string order of station_id, then by UTC instant.

`stats.json` should be ordinary JSON. The top-level keys need to be spelled exactly like this: duplicate_rows_dropped, skipped_malformed_rows, skipped_suppressed_rows, shifted_nonexistent_timestamps, deduped_row_count, station_count, median_temperature_c_all, global_quality_counts, and stations, where the first six counts are integers and median_temperature_c_all is a string in the same one-decimal style as temperature_c in the CSV. Both global_quality_counts and each station’s quality_counts are small objects with exactly OK, WARN, and EST as integer counts. The stations list should be sorted by station_id, and each station object must use exactly these keys—no synonyms—including avg_temperature_c for the mean temperature string (not average_temperature_c), plus station_id, readings as an integer count of kept rows, min_temperature_c, max_temperature_c, median_temperature_c, quality_counts, longest_gap_minutes as the biggest whole-minute gap between consecutive kept UTC times or null when there’s only one reading, and quality_runs as a single JSON integer: sort that station’s kept readings by UTC, walk in order, and set it to one plus how many times the quality code changes from the previous row (a single reading gives you 1). For medians over numbers, sort them and average the two middle values when there’s an even count, then format min_temperature_c, max_temperature_c, median_temperature_c, avg_temperature_c, and median_temperature_c_all the same way you formatted temperature_c in the CSV.
